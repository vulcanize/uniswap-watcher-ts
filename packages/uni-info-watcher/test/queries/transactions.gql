query transactions($block: Block_height, $first: Int, $skip: Int){
  transactions(block: $block, first: $first, skip: $skip){
    burns {
      id
    }
    id
    mints {
      id
    }
    swaps {
      id
    }
    timestamp
    blockNumber
    gasUsed
    gasPrice
  }
}
